---
id: "2026-07-25-安全整合上游-6e38642"
title: "安全整合上游 6e38642"
status: "in_progress"
created: "2026-07-25"
updated: "2026-07-25"
category: "架构"
tags: ["upstream", "pager", "pi-grok", "sync"]
---

# Issue: 安全整合上游 6e38642

## Goal

在不丢失任何本地提交或 Pi-Grok 集成能力的前提下，安全吸收 upstream
`6e386420825bd44ae648c63e7c8cba12fcec9401`（SOURCE_REV
`9b8d35b46d959c042ea9aa31cbbebbd1f0c5c527`）的两次 monorepo 同步。

上游变更记录：[`docs/upstream/UPSTREAM_CHANGELOG.md`](../../upstream/UPSTREAM_CHANGELOG.md) `[6e38642]`。

## Preflight facts

| 项 | 值 |
|---|---|
| 本地基线 | `main` @ `57b0cc709db37d2a5eacf35ef9826905e6bbe69a` |
| origin 状态 | `main` 与 `origin/main` 一致 |
| 工作区 | clean；无未跟踪文件 |
| upstream base | `a5727c5960452e7527a154b25cb5bf00cda0545e` |
| upstream tip | `6e386420825bd44ae648c63e7c8cba12fcec9401` |
| pending range | `a5727c5..6e38642`，2 commits |
| diff | 349 files，+27899/−10881 |
| 高风险区域 | Pager app/session/queue/voice/settings/workflow；Shell ACP/auth/workflow |

## Architecture invariants

1. Grok Pager 仍是唯一可见 TUI。
2. Pi 仍是唯一 Agent Core；queue、session、trust、tools、extensions 语义归 Pi。
3. `pi-grok-adapter` 保持 headless、library-only，不吸收 Shell/Pager 所有权。
4. 复用上游 Pager 核心逻辑，同时外科式保留 Pi-Grok 窄接缝。
5. 不修改 `pi-main` 源码扩 RPC；不弱化 source-identity verifier。

## Plan

### Phase 1 — Record and backup

- [x] Fetch `upstream/main` and record authoritative `Changes:` lists
- [x] Prepend `[6e38642]` to `docs/upstream/UPSTREAM_CHANGELOG.md`
- [x] Create this Issue as the task SSOT
- [ ] Commit the documentation-only pre-merge record
- [ ] Create a backup ref for the exact pre-merge `main`

### Phase 2 — Isolated merge

- [ ] Create `sync/upstream-6e38642` in an isolated worktree
- [ ] Merge `upstream/main` without rewriting either history
- [ ] Inventory every conflict before resolving
- [ ] Prefer upstream core logic; reapply only declared Pi-Grok seams

### Phase 3 — Seam and metadata audit

- [ ] Audit `external_agent`, OpenPiConfig, `project_config_dir`, DirectPi/queue/session/trust/model-picker seams
- [ ] Review Pager `app/`, actions/effects/event loop/session/queue/voice/workflow overlap
- [ ] Update `SOURCE_REV` and `AGENTS.md` base only after the merge is coherent
- [ ] Deliberately update source-identity/renderer baselines only when required; never broaden allowlists casually
- [ ] Ensure `pi-main` gitlink/model catalog changes are intentional or left untouched

### Phase 4 — Verification

- [ ] Review complete diff and conflict resolutions
- [ ] `cargo test -p pi-grok-adapter`
- [ ] `cargo test -p xai-grok-pager-bin --bin grok-pi`
- [ ] `cargo check -p xai-grok-pager-bin --bin grok-pi`
- [ ] `./build.sh`
- [ ] Run relevant architecture/source-identity checks from `./verify.sh`, recording known infrastructure blockers separately
- [ ] Confirm clean `git status` and expected merge graph

### Phase 5 — Delivery

- [ ] Commit focused conflict/seam fixes separately from generated baseline or documentation updates when practical
- [ ] Update this Issue and changelog status with actual commits and verification evidence
- [ ] Fast-forward `main` only after the isolated branch is verified
- [ ] Do not push or force-update any remote

## Acceptance criteria

- No local commit, tracked change, untracked file, submodule state, or Pi-Grok seam is lost.
- Merge history retains upstream ancestry; no rebase, squash, force push, or public-history rewrite.
- `main` stays unchanged by the risky merge until the isolated branch passes the agreed verification gates.
- `SOURCE_REV` and documented upstream base exactly match `6e38642` after delivery.
- Passing checks and pre-existing/infrastructure blockers are reported separately with command evidence.
- Final working tree is clean and the backup ref remains available for rollback.

## Rollback

- Before the merge, create a named backup branch at the pre-merge `main` commit.
- Perform all risky work in a separate worktree/branch.
- If validation fails irreparably, remove the isolated worktree with `git worktree remove` only after confirming no unique changes are uncommitted; keep the backup branch and main untouched.
- Never use `git reset --hard`, broad restore, force checkout, or force push.

## Errors encountered

| Date | Error | Resolution |
|---|---|---|
| 2026-07-25 | Initial Herdr launch used an invalid remote-side path | Relaunched with `~/dev/ai/pi-grok-build`; Herdr resolved `/Users/dengwenyu/Dev/AI/pi-grok-build` |
| 2026-07-25 | One GPTxPi continuation timed out/aborted while reading prior records | Verified no write occurred, re-bound the same session, and resumed from collected facts |

## Status log

- **[2026-07-25]** Preflight complete; upstream range and authoritative changelog facts recorded; Issue opened → **in progress**.
