---
id: "2026-07-25-安全整合上游-6e38642"
title: "安全整合上游 6e38642"
status: "in_progress"
created: "2026-07-25"
updated: "2026-07-25"
category: "架构"
tags: ["upstream", "pager", "pi-grok", "sync"]
---

# Issue: 安全整合上游 6e38642

## Goal

在不丢失任何本地提交或 Pi-Grok 集成能力的前提下，安全吸收 upstream
`6e386420825bd44ae648c63e7c8cba12fcec9401`（SOURCE_REV
`9b8d35b46d959c042ea9aa31cbbebbd1f0c5c527`）的两次 monorepo 同步。

上游变更记录：[`docs/upstream/UPSTREAM_CHANGELOG.md`](../../upstream/UPSTREAM_CHANGELOG.md) `[6e38642]`。

## Result so far

| 项 | 值 |
|---|---|
| 预合并文档提交 | `3b15789bcfa3839ac3570179a3ff7d42fc1be6a4` |
| 回滚分支 | `backup/pre-upstream-6e38642` → `3b15789` |
| 隔离分支 / worktree | `sync/upstream-6e38642` / `/Users/dengwenyu/Dev/AI/pi-grok-build-sync-6e38642` |
| 双亲 merge commit | `963ccf5cf9bd76e90c9dcb5a9f57254fcdd784a7` |
| merge parents | `3b15789` + upstream `6e38642` |
| 冲突 | 16 files；全部逐文件审计并解决 |
| `SOURCE_REV` | `9b8d35b46d959c042ea9aa31cbbebbd1f0c5c527` |
| `AGENTS.md` base | `6e386420825bd44ae648c63e7c8cba12fcec9401` |
| `pi-main` gitlink | 保持 `a5afc3f171e422e08a2ccc342827719f9952f38a`，未改动 |
| 当前状态 | 隔离分支已验证；等待 `main` ff-only 交付 |

## Architecture invariants

1. Grok Pager 仍是唯一可见 TUI。
2. Pi 仍是唯一 Agent Core；queue、session、trust、tools、extensions 语义归 Pi。
3. `pi-grok-adapter` 保持 headless、library-only，不吸收 Shell/Pager 所有权。
4. 复用上游 Pager 核心逻辑，同时外科式保留 Pi-Grok 窄接缝。
5. 不修改 `pi-main` 源码扩 RPC；不弱化 source-identity verifier。

## Conflict resolution

- Pager：保留 `external_agent`、OpenPiConfig、DirectPi、Pi session/tree/queue/settings、产品隔离与外部 SessionPicker；吸收 tutorial、privacy banner、voice keybind、slash tags、send-now 失败回队和上游 queue/resume 变化。
- Workflow：保留 `WorkflowAgentBackend` / `WorkflowAgentSpawnRequest` / Pi Spawn 所有权，并适配上游 `SubagentSpawnRequest` 信封与父会话隔离的 cancel 契约。
- Hooks：保留 `project_config_dir(root)/hooks` 产品隔离，吸收上游 `include_cursor` 语义。
- Update：保留 `pi_update` 双更新能力，采用上游 `version_policy`，移除已删除的 `minimum_version` 导出。
- Baseline：未扩大 verifier allowlist；剩余 source/renderer/slash 失败已在合并前 `main` 上同样复现，另行治理。

## Plan

### Phase 1 — Record and backup

- [x] Fetch `upstream/main` and record authoritative `Changes:` lists
- [x] Prepend `[6e38642]` to `docs/upstream/UPSTREAM_CHANGELOG.md`
- [x] Create this Issue as the task SSOT
- [x] Commit the documentation-only pre-merge record (`3b15789`)
- [x] Create `backup/pre-upstream-6e38642`

### Phase 2 — Isolated merge

- [x] Create `sync/upstream-6e38642` in an isolated worktree
- [x] Merge `upstream/main` without rewriting either history
- [x] Inventory all 16 conflicts before resolving
- [x] Prefer upstream core logic and reapply declared Pi-Grok seams
- [x] Create two-parent merge commit `963ccf5`

### Phase 3 — Seam and metadata audit

- [x] Audit `external_agent`, OpenPiConfig, `project_config_dir`, DirectPi/queue/session/trust/model-picker seams
- [x] Review Pager `app/`, actions/effects/event loop/session/queue/voice/workflow overlap
- [x] Update `SOURCE_REV` and `AGENTS.md` base after coherent merge
- [x] Preserve the `pi-main` gitlink and avoid generated model-catalog drift
- [x] Compare verifier failures against pre-merge `main`; do not broaden allowlists for pre-existing drift

### Phase 4 — Verification

- [x] `git diff --check` and complete conflict/index review
- [x] `cargo check -p xai-grok-pager-bin --bin grok-pi`
- [x] `cargo test -p pi-grok-adapter` — 128 passed
- [x] `env -u PI_GROK_NATIVE_COMMANDS cargo test -p xai-grok-pager-bin --bin grok-pi -- --test-threads=1` — 56 passed
- [x] `./build.sh` — PASS; built `target/debug/grok-pi`
- [x] Workflow focused tests — 73 passed / 1 known macOS `/var` → `/private/var` canonical-path assertion failure
- [x] Run `./verify.sh` and remaining stages; record pre-existing/infrastructure blockers separately
- [x] Confirm clean isolated branch and expected merge graph

## Verification blockers (not introduced by this merge)

| Check | Result | Evidence |
|---|---|---|
| Source identity / renderer baselines | FAIL | Same missing/hash/addition sets reproduce on pre-merge `main`; manifests are stale |
| Slash native-command verifier | FAIL | Same stale expected command list and `fork`/`voice` prohibition reproduce on pre-merge `main` |
| Mock `agent_settled` static token | FAIL | Mock expects literal `"agent_settled" => self.finish_prompt`; current structured handler is identical before/after merge and runtime mock events pass |
| Rust syntax verifier | NOT RUN | Python `tree_sitter` module is not provisioned |
| Pager focused lib tests | BLOCKED | Existing `xai-grok-pager-pty-harness` compile errors (`PtyPump`, duplicate `wait_for_text`, missing `pump_one`) |
| Workflow symlink-path test | 73/74 | macOS aliases `/var/...` to `/private/var/...`; exact test reproduces |

## Phase 5 — Delivery

- [x] Keep merge and documentation changes in focused commits
- [x] Update this Issue and changelog with merge/verification evidence
- [ ] Fast-forward `main` to the verified sync branch
- [x] Do not push or force-update any remote
- [ ] Record final `main` commit and clean status

## Acceptance criteria

- No local commit, tracked change, untracked file, submodule state, or Pi-Grok seam is lost.
- Merge history retains upstream ancestry; no rebase, squash, force push, or public-history rewrite.
- `main` stays unchanged by the risky merge until the isolated branch passes the agreed verification gates.
- `SOURCE_REV` and documented upstream base exactly match `6e38642` after delivery.
- Passing checks and pre-existing/infrastructure blockers are reported separately with command evidence.
- Final working tree is clean and the backup ref remains available for rollback.

## Rollback

- `backup/pre-upstream-6e38642` preserves the exact pre-merge integration baseline at `3b15789`.
- The merge was developed and validated only in the isolated worktree.
- No destructive Git command, force push, rebase, or public-history rewrite was used.

## Errors encountered

| Date | Error | Resolution |
|---|---|---|
| 2026-07-25 | Initial Herdr launch used an invalid remote-side path | Relaunched against the resolved absolute project path |
| 2026-07-25 | GPTxPi continuations restarted while the merge was in progress | Resumed from the persistent isolated worktree; verified each accepted write before continuing |
| 2026-07-25 | Upstream changed workflow subagent mailbox contracts | Adapted the existing Pi-owned backend wrapper without transferring ownership to stock Grok coordinator |
| 2026-07-25 | Parallel env-mutating binary test produced one false failure | Re-ran the complete binary test serially with the flag unset; 56/56 passed |

## Status log

- **[2026-07-25]** Preflight, changelog, Issue, backup branch and isolated worktree created.
- **[2026-07-25]** 16 conflicts resolved; Pi-Grok seams preserved; merge commit `963ccf5` created.
- **[2026-07-25]** Compile, adapter tests, serial binary tests and build passed; remaining verifier failures proven pre-existing → **ready for ff-only delivery**.
