---
id: "2026-07-29-sync-upstream-5da6962-pr"
title: "同步上游 Grok Build 至 5da6962 并通过 PR 交付"
status: "in-progress"
created: "2026-07-29"
updated: "2026-07-29"
category: "architecture"
tags: ["upstream", "merge", "worktree", "github-pr", "grok-pi"]
---

# Issue: 同步上游至 5da6962 并通过 PR 交付

## Goal

在隔离工作树中把 `xai-org/grok-build` 的 `47348d1..5da6962` 三个同步提交合入 Grok-Pi，保留 Pi-Grok 产品语义，完成验证后通过 GitHub PR 流程交付到 `Dwsy/grok-pi:main`。

现有 PR：`Dwsy/grok-pi#3`（`xai-org/grok-build:main` → `Dwsy/grok-pi:main`）。该 PR 当前为 `dirty`，不能直接在 GitHub 上完成冲突合并，因此以它作为上游来源与审查记录，在本地隔离分支完成语义冲突解决和验证后更新目标分支，使 PR 反映已吸收的上游提交。

## Inputs

- Base: `47348d13ec4508dcfe440e34c6d511bb02998fb2`
- Upstream tip: `5da6962e4adb9c857f3def762542b52b4ec3e522`
- SOURCE_REV: `2a818575225183d8ca915f5632a09b8067b5156a`
- Diff: 315 files, +22006 / −7473
- Local integration base: `main` at `cb8b634`

## Constraints

- 主工作树只负责记录、审查和最终 ff-only 交付；冲突解决必须在新的隔离工作树完成。
- 保留上游 ancestry，使用双亲 merge commit，不 squash 上游同步提交。
- Pi 继续拥有 Pi session、模型、工具、资源和 extension runner；adapter 只负责协议/语义投影。
- 不盲目接受上游或本地整文件版本；所有冲突按功能 seam 合并。
- `SOURCE_REV`、基线和关闭文档只在验证完成后更新。

## High-risk seams

- Pager `app/`、dispatch、settings/model background startup、plan/review scrollback、terminal input。
- Shell session lifecycle、subagent process scope、session load/fork memory、search recovery。
- Tools/MCP/LSP/subagent close-time resource reclamation。
- Upstream Herdr multiplexer detection与本 fork 已有 Herdr 集成。
- Pi-Grok adapter、注入扩展、F2 设置、`.grok-pi` 隔离路径与 `pi-main` gitlink。

## Phases

- [x] 删除全部旧 linked worktree，仅保留主工作树。
- [x] 获取并记录上游变更到 `docs/upstream/UPSTREAM_CHANGELOG.md`。
- [ ] 创建 `sync/upstream-5da6962` 隔离工作树与安全基线。
- [ ] 合并 `upstream/main`，逐项解决冲突并审计无冲突遗漏。
- [ ] 运行 adapter、Pager、Shell、Tools、配置及产品构建验证。
- [ ] 更新 SOURCE_REV/上游记录状态和验证说明。
- [ ] ff-only 交付到 `main`，推送 fork，并用 `gh` 核对/完成 PR #3 状态。

## Acceptance

- [ ] merge commit 的第二父提交为 `5da6962`，上游 ancestry 可追踪。
- [ ] 所有冲突 marker、未合并路径和意外 fork-only 删除均为零。
- [ ] Pi-Grok 产品 seam 与上游新增行为同时保留。
- [ ] 关键测试、`cargo check`、`./build.sh` 和 diff hygiene 通过，已知非回归 blocker 单独记录。
- [ ] `main` 工作树干净，GitHub PR 状态与实际交付一致。
