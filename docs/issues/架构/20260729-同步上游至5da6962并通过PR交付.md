---
id: "2026-07-31-sync-upstream-dd04f39"
title: "同步上游 Grok Build 至 dd04f39"
status: "in-progress"
created: "2026-07-29"
updated: "2026-07-31"
category: "architecture"
tags: ["upstream", "merge", "worktree", "github-pr", "grok-pi"]
---

# Issue: 同步上游至 dd04f39

## Goal

在隔离工作树中把 `xai-org/grok-build` 的 `47348d1..dd04f39` 五个同步提交合入 Grok-Pi，保留 Pi-Grok 产品语义，并通过本地验证后以 ff-only 方式交付到本地 `main`。

该 Issue 最初以 `5da6962` 为目标；2026-07-31 抓取上游后目标前进到 `dd04f39`，因此沿用同一追踪记录扩展范围。远端 PR/推送不在本次默认授权内，除非用户另行明确要求。

## Inputs

- Base: `47348d13ec4508dcfe440e34c6d511bb02998fb2`
- Upstream tip: `dd04f397b1d02f2272b092555669dfba1f01bc85`
- SOURCE_REV: `2a28b4a86cfc4a4c133c35b7fc2a6a9964387c39`
- Diff: 618 files, +55053 / −18012
- Local integration base: `main` at `84e1fbf`
- Fork/upstream overlap: 102 files

## Constraints

- 主工作树只负责记录、审查和最终 ff-only 交付；冲突解决必须在新的隔离工作树完成。
- 保留上游 ancestry，使用双亲 merge commit，不 squash 上游同步提交。
- Pi 继续拥有 Pi session、模型、工具、资源和 extension runner；adapter 只负责协议/语义投影。
- 不盲目接受上游或本地整文件版本；所有冲突按功能 seam 合并。
- `SOURCE_REV`、基线和关闭文档只在验证完成后更新。

## High-risk seams

- Pager `app/`、dispatch、settings/model background startup、plan/review scrollback、terminal input 与 headless 模块拆分。
- External ACP 的 `session/list`、tool-call streaming、gateway task lifecycle 与 history trailer。
- Shell session lifecycle、subagent process scope、session load/fork memory、search recovery与 persistence actor。
- Tools/MCP/LSP/PTTY/hooks/subagent close-time resource reclamation。
- Sampling/compaction 模块重构与 Grok-Pi model/thinking/context 投影。
- Upstream Herdr multiplexer detection 与本 fork 已有 Herdr 集成。
- Pi-Grok adapter、注入扩展、F2 设置、`.grok-pi` 隔离路径与 `pi-main` gitlink。

## Phases

- [ ] 检查并移除旧 `sync/upstream-5da6962` linked worktree。
- [x] 获取并记录上游变更到 `docs/upstream/UPSTREAM_CHANGELOG.md`。
- [ ] 创建 `sync/upstream-dd04f39` 隔离工作树与安全基线。
- [ ] 合并 `upstream/main`，逐项解决冲突并审计无冲突遗漏。
- [ ] 运行 adapter、Pager、Shell、Tools、配置及产品构建验证。
- [ ] 更新 SOURCE_REV/上游记录状态和验证说明。
- [ ] 验证完成后以 ff-only 方式交付到本地 `main`；不执行 push 或远端 PR 操作。

## Acceptance

- [ ] merge commit 的第二父提交为 `dd04f39`，上游 ancestry 可追踪。
- [ ] 所有冲突 marker、未合并路径和意外 fork-only 删除均为零。
- [ ] Pi-Grok 产品 seam 与上游新增行为同时保留。
- [ ] 关键测试、`cargo check`、`./build.sh` 和 diff hygiene 通过，已知非回归 blocker 单独记录。
- [ ] `main` 工作树干净；仅在用户明确授权后才 push 或操作远端 PR。
