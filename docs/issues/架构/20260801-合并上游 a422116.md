---
id: "2026-08-01-合并上游 a422116"
title: "合并上游 a422116"
status: "in_progress"
created: "2026-08-01"
updated: "2026-08-01"
category: "架构"
tags: ["workhub", "合并上游 a422116"]
---

# Issue: 合并上游 a422116

## Goal

在不破坏 Pi-Grok 原生 Pager 接缝的前提下，将 `upstream` 的 `a4221165824e5b1f5c4c10b7459f65e78dd6448d` 集成到本地 `main`，并完成窄范围验证。

## 背景/问题

当前 `main` 已包含上游基线 `dd04f39` 及其本地集成提交；`a422116` 带来 165 个文件的 Shell/Pager 生命周期、任务、压缩、权限和 PTY 变化。高风险接缝集中在 Pager `app/`、session/event-loop/effects 与 Shell session/compaction，必须先记录变更，再在隔离工作树解析冲突。

## 验收标准 (Acceptance Criteria)

- [ ] `docs/upstream/UPSTREAM_CHANGELOG.md` 记录 `dd04f39..a422116` 的权威 `Changes:`、统计和风险。
- [ ] 隔离集成分支包含 `a422116` 的上游父提交，且 Pi-Grok 的 adapter headless、Pi agent/session ownership 与 Pager 唯一 TUI 不变。
- [ ] `SOURCE_REV` 更新为 `8d69c91f02bcacf01e98d5aebbf2f92547c45738`，旧 `dd04f39` 条目关闭为已合并。
- [ ] 相关 adapter 单测、`grok-pi` binary check/test 及必要的 diff/架构检查已执行并读取结果；已知基线 blocker 单独记录。
- [ ] 本地 `main` 只接收已验证的集成结果，不 push 远端。

## 实施阶段

### Phase 1: 规划和准备
- [x] 分析需求和依赖
- [x] 记录上游变更清单与 165 文件统计
- [x] 确定隔离工作树和验证计划

### Phase 2: 执行
- [ ] 创建隔离集成分支/工作树
- [ ] 合并 `a422116`，保留必要的 Pi-Grok 接缝
- [ ] 审查冲突、source identity 与 diff 范围

### Phase 3: 验证
- [ ] 运行 adapter 相关测试
- [ ] 运行 `grok-pi` binary check/test
- [ ] 运行必要的架构、格式和文档一致性检查

### Phase 4: 交付
- [ ] 关闭 upstream changelog pending 状态并更新 `SOURCE_REV`
- [ ] 将已验证结果快进到本地 `main`
- [ ] 更新 Issue 状态和验证记录

## 关键决策

| 决策 | 理由 |
|------|------|
| 先写 upstream changelog，再执行 merge | 项目同步流程要求以提交消息 `Changes:` 作为合并前事实记录 |
| 在隔离工作树合并，再回接 `main` | Pager/Shell 变化大，避免未验证冲突污染主工作树 |
| 保留上游核心语义，只重应用 Pi-Grok 窄接缝 | 保持未来上游同步友好，同时维持 Pi 单一 agent core 与 Pager 单一 TUI |
| 不改 `pi-main` 和 `pi-grok-adapter` 的职责边界 | 本提交未触及 adapter；协议能力仍应通过现有官方扩展/桥接边界承载 |

## 遇到的错误

| 日期 | 错误 | 解决方案 |
|------|------|---------|
| — | — | — |

## 相关资源

- [x] `docs/upstream/UPSTREAM_CHANGELOG.md`
- [x] `NATIVE_GROK_TUI_ALIGNMENT.md`
- [x] `FEATURE_MATRIX.md`
- [ ] 集成分支验证结果

## Notes

- 目标提交：`a4221165824e5b1f5c4c10b7459f65e78dd6448d`；其 `Source-Revision` 为 `8d69c91f02bcacf01e98d5aebbf2f92547c45738`。
- 当前工作树初始干净；本地 `main` 已包含 `360f801` 对 `dd04f39` 的集成及其后的 Pi-Grok 提交。
- 当前 upstream 变更与本地 `dd04f39` 之后的改动在 10 个文件相交，主要是 Pager app/dispatch/effects 和 `Cargo.lock`。

---

## Status 更新日志

- **2026-08-01**: 状态变更 → `in_progress`，备注: 已记录 upstream changelog，开始隔离集成。