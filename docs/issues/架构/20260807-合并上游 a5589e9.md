---
id: "2026-08-07-合并上游 a5589e9"
title: "合并上游 a5589e9"
status: "done"
created: "2026-08-07"
updated: "2026-08-07"
category: "架构"
tags: ["workhub", "upstream-sync", "a5589e9"]
---

# Issue: 合并上游 a5589e9

## Goal

在不破坏 Pi-Grok 架构边界和本地主工作树未提交改动的前提下，将 `upstream` 的 `a5589e958437d79e13db026eedcb1720bffd4063` 集成到隔离分支，并完成可审计的冲突解析与分层验证。

## 背景/问题

当前本地基线为 `a4221165824e5b1f5c4c10b7459f65e78dd6448d`，实际 merge 第一父提交为 `8c138e7af38f8b33f990b981b563ce5861e2a6c5`。待同步范围包含 4 个上游提交、577 个文件、`+44078/-17588`，重点覆盖 Pager、Shell session/workflow、queue、dashboard、permission、terminal teardown 与 ACP session operations。

主工作树存在未提交的 adapter/tool projection 改动和 upstream changelog 记录。本次只在 `/Users/dengwenyu/Dev/AI/pi-grok-build-sync-a5589e9` 的 `sync/upstream-a5589e9` 分支执行，主工作树和远端保持不动。

## 验收标准 (Acceptance Criteria)

- [x] `docs/upstream/UPSTREAM_CHANGELOG.md` 已记录 `a422116..a5589e9` 的权威 `Changes:`、统计与风险，合并前状态为 `Recorded for review; not merged`。
- [x] 建立隔离工作树和独立分支，起点精确为 `8b5ecfee`，主工作树三处未提交改动保持不变。
- [x] 生成包含上游 `a5589e9` 的两亲 merge commit，并逐文件解析 20 个预演冲突。
- [x] Pi 继续拥有 agent/session/queue 语义；Pager 继续是唯一 TUI；`pi-grok-adapter` 继续 headless、library-only。
- [x] `pi-main` gitlink 不变，不用上游 ACP session/resume/close 替代 Pi session owner。
- [x] 更新 `SOURCE_REV`、`AGENTS.md` base；无需扩大 verifier allowlist 或刷新无关基线。
- [x] 运行 adapter 单测、`grok-pi` binary check/test、格式和架构/差异审查；PASS 与既有全量测试红灯分开记录。
- [ ] 只在用户明确确认后将验证结果交付到本地 `main`；不 push 远端。

## 预演冲突

20 个冲突文件集中在：

- Pager input/render/viewer/app_view/dashboard。
- Session fork/lifecycle/load、foreign sessions、session picker、welcome。
- Effects、modals、permission/session tests。
- 删除后被上游修改的 `tests/pty_e2e_queue.rs`。
- Shell `config/mod.rs` 与 workflow manager。

## 关键决策

| 决策 | 理由 |
|---|---|
| 上游核心结构优先，Pi-Grok 接缝外科重放 | 降低未来同步冲突，同时保持产品边界 |
| 不直接修改 `main` | 主工作树有未提交改动，隔离是数据安全前提 |
| 不初始化或修改 `pi-main`，除非验证明确需要 | 本范围不应改变 Pi 源码或 gitlink |
| 删除/移动测试文件遵循上游测试布局 | 避免恢复已废弃的聚合入口；Pi 特有断言迁移到现有测试模块 |
| source identity 只更新事实基线，不扩大 allowlist | 新接缝必须显式审查，不能用宽松规则掩盖差异 |

## 回滚计划

- 合并未提交前：在隔离工作树执行 `git merge --abort`。
- 合并提交后：删除隔离 worktree/分支即可；不影响 `main`。
- 不使用 `reset --hard`、广域 restore 或 force checkout。
- 主工作树的三处未提交改动不参与本分支提交。

## 实施阶段

### Phase 1: 规划和准备
- [x] 核对 commit range、changelog、工作树和冲突预演
- [x] 建立隔离分支与回滚边界
- [x] 提交规划文档和 changelog 记录

### Phase 2: 合并和冲突解析
- [x] 合并 `a5589e9`
- [x] 逐文件比较 stage 1/2/3，保留本地语义并吸收上游结构
- [x] 审查删除/新增文件与 Pi-Grok 窄接缝

### Phase 3: 元数据和验证
- [x] 更新 `SOURCE_REV`、`AGENTS.md` base、changelog 状态；未扩大 verifier allowlist
- [x] 运行窄测试、binary check/test、fmt 和差异审查
- [x] 审查最终 diff、merge parents、submodule gitlink 与主工作树状态

### Phase 4: 交付
- [x] 回写 Result/Verification
- [x] 隔离分支作为本地交付记录保留
- [ ] 等待用户确认是否 ff-only 交付到 `main`

## Result

| 项目 | 结果 |
|---|---|
| 隔离分支 / worktree | `sync/upstream-a5589e9` / `/Users/dengwenyu/Dev/AI/pi-grok-build-sync-a5589e9` |
| merge parents | `8c138e7af38f8b33f990b981b563ce5861e2a6c5` + `a5589e958437d79e13db026eedcb1720bffd4063` |
| `SOURCE_REV` | `4d6d11372ab8f73026a78c45a7b7e7b1310eb39f` |
| `AGENTS.md` base | `a5589e958437d79e13db026eedcb1720bffd4063` |
| `pi-main` gitlink | 保持 `4f0437e2d58d651dd934119ecabea2893975f62f` 不变 |
| 本地 `main` / 远端 | 均未修改；未 push |

冲突解析采用“上游结构优先、Pi-Grok 接缝外科重放”。保留 Pi 对 agent/session/queue 的所有权、Pager 唯一 TUI、headless adapter、SessionTree/remote-TUI/modal/branding 等本地产品接缝；未引入第二套 UI，也未扩大 verifier allowlist。

## Verification

- `./scripts/cargo-shared.sh test -p pi-grok-adapter`：PASS，141 tests。
- `./scripts/cargo-shared.sh test -p xai-grok-pager-bin --bin grok-pi`：PASS，72 tests。
- `./scripts/cargo-shared.sh check -p xai-grok-pager-bin --bin grok-pi`：PASS。
- Viewer、plan approval model picker、remote-TUI paste、jump/rewind、permission sticky cursor、session-info 与 startup snapshot 等定向回归：PASS。
- Pager 全量 lib 并行基线：合并前 `8368 passed / 42 failed / 10 ignored`；合并后 `8369 passed / 41 failed / 10 ignored`。失败集合受共享全局状态影响而漂移；本轮新增候选均以 `--exact --test-threads=1` 单独复跑通过，未发现确定性的同步新增失败。
- `git ls-files -u` 为空；`git diff --check`、`git diff --cached --check` 通过；无冲突标记；`pi-main` gitlink 未变化。

## Notes

- 上游 tip: `a5589e958437d79e13db026eedcb1720bffd4063`。
- 上游 `SOURCE_REV`: `4d6d11372ab8f73026a78c45a7b7e7b1310eb39f`。
- 预演命令：`git merge-tree --write-tree 8b5ecfee a5589e9`。
- 隔离 merge 完成后仍需用户明确确认，才可 ff-only 交付到本地 `main`。
