---
id: "2026-07-26-安全整合上游-47348d1"
title: "安全整合上游 47348d1"
status: "in_progress"
created: "2026-07-26"
updated: "2026-07-26"
category: "架构"
tags: ["upstream", "pager", "pi-grok", "sync", "security"]
---

# Issue: 安全整合上游 47348d1

## Goal

在不丢失任何本地提交、未提交工作或 Pi-Grok 集成能力的前提下，安全吸收 upstream `47348d13ec4508dcfe440e34c6d511bb02998fb2`（SOURCE_REV `d02693a856a54f1030695b36b91d276e96b30b23`），完成验证后再交付到本地 `main`。

上游变更记录：[`docs/upstream/UPSTREAM_CHANGELOG.md`](../../upstream/UPSTREAM_CHANGELOG.md) `[47348d1]`。

## Inputs and safety baseline

| Item | Value |
|---|---|
| Current fork baseline | `906470c8e9f59476ee51ace190f09f84b335de89` |
| Last synced upstream | `6e386420825bd44ae648c63e7c8cba12fcec9401` |
| New upstream tip | `47348d13ec4508dcfe440e34c6d511bb02998fb2` |
| SOURCE_REV | `d02693a856a54f1030695b36b91d276e96b30b23` |
| Upstream diff | 138 files, +7283 / -5796 |
| Dirty-work snapshot | `stash@{0}` at creation, object `97f443b57a7b3a8d9fd609dc7c29812fcae27791`; includes tracked and untracked files |
| Isolated branch | `sync/upstream-47348d1` |
| Isolated worktree | `/Users/dengwenyu/Dev/AI/pi-grok-build-sync-47348d1` |
| Delivery policy | No push, no rebase, no force update, no destructive restore |

## Architecture invariants

1. Grok Pager remains the only visible TUI.
2. Pi remains the only Agent Core; queue, session, trust, tools, extensions, compaction and retries stay Pi-owned.
3. `pi-grok-adapter` remains headless and library-only.
4. Upstream core behavior is preferred, while declared Pi-Grok seams are reapplied surgically.
5. Product isolation (`~/.grok-pi`, `<repo>/.grok-pi`) and `project_config_dir()` routing must not regress.
6. Pi source is not modified to expand RPC; source-identity verification is not weakened broadly.
7. The pre-existing dirty work is restored after delivery and remains backed by the named stash until final verification.

## Upstream risk map

| Area | Files | +/- | Risk to grok-pi |
|---|---:|---:|---|
| Pager (TUI) | 69 | +2181/-2148 | `app/`, questions/paste, task output, session visibility, terminal/status rendering and external-profile seams |
| Shell (agent runtime) | 30 | +1891/-2608 | runtime/thread recovery, HTTP construction, SessionEnd lifecycle and terminal transport |
| Workspace / Permission | 16 | +1334/-325 | security confinement and `acceptEdits` approval boundary |
| Config | 9 | +1039/-138 | managed configuration signing, embedded key and remote verification kill-switch |
| Hooks / Plugins | 7 | +722/-414 | config hook parsing, global hook root and product-isolated hook paths |
| Agent lifecycle | 2 | +40/-148 | session/runtime failure recovery |

## Plan

### Phase 1 — Record and backup

- [x] Fetch `upstream/main` and resolve `6e38642..47348d1`
- [x] Transcribe the upstream `Changes:` list into `docs/upstream/UPSTREAM_CHANGELOG.md`
- [x] Create this Issue as the task SSOT
- [x] Snapshot all tracked and untracked local work without dropping it
- [x] Create isolated branch/worktree from clean `main`
- [ ] Commit the documentation-only pre-merge record
- [ ] Create `backup/pre-upstream-47348d1`

### Phase 2 — Isolated merge

- [ ] Merge `upstream/main` with preserved two-parent history
- [ ] Inventory every conflict before resolving any file
- [ ] Compare stage 1/2/3 for each conflict
- [ ] Preserve Pi-Grok seams while absorbing upstream core fixes
- [ ] Create the two-parent merge commit

### Phase 3 — Seam and metadata audit

- [ ] Audit `external_agent`, OpenPiConfig, DirectPi, queue/session/tree/trust/model/settings seams
- [ ] Audit product-isolated home/project paths and hook discovery
- [ ] Audit Pager question/paste, task output and terminal disconnect behavior
- [ ] Audit managed-config signing and security boundaries
- [ ] Update `SOURCE_REV`, `AGENTS.md` base and verifier baselines only after coherent merge
- [ ] Preserve the `pi-main` gitlink and reject generated model-catalog drift

### Phase 4 — Verification

- [ ] `git diff --check` and conflict/index review
- [ ] `cargo check -p xai-grok-pager-bin --bin grok-pi`
- [ ] `cargo test -p pi-grok-adapter`
- [ ] `env -u PI_GROK_NATIVE_COMMANDS cargo test -p xai-grok-pager-bin --bin grok-pi -- --test-threads=1`
- [ ] `./build.sh`
- [ ] `./verify.sh`, with pre-existing/infrastructure blockers reported separately
- [ ] Focused checks for external-agent seams, F2 toggles, product isolation, hooks and session/queue behavior
- [ ] Review final diff, submodule state, generated files and merge graph

### Phase 5 — Delivery

- [ ] Update this Issue and changelog with merge/verification evidence
- [ ] Keep commits focused (record, merge, verification closeout)
- [ ] Fast-forward or merge the verified integration tip into local `main`
- [ ] Restore the pre-existing dirty snapshot and resolve any apply conflicts without losing either side
- [ ] Re-run affected checks after snapshot restoration where necessary
- [ ] Keep the safety stash until restored content is verified
- [ ] Do not push any remote

## Acceptance criteria

- No local commit, tracked modification, untracked file, submodule state or declared Pi-Grok seam is lost.
- Merge history retains upstream ancestry; no squash, rebase, force push or public-history rewrite is used.
- Risky merge work occurs only in the isolated worktree; `main` receives only the verified integration tip.
- `SOURCE_REV` and documented upstream base exactly match `47348d1` after delivery.
- Upstream security and lifecycle fixes are present without transferring Pi-owned responsibilities to stock Grok components.
- Passing checks and pre-existing/infrastructure blockers are separated with command evidence.
- The original dirty work is restored on `main` and remains recoverable from stash object `97f443b57a7b3a8d9fd609dc7c29812fcae27791` during final audit.
- No remote is pushed or force-updated.

## Rollback

- `backup/pre-upstream-47348d1` will preserve the exact documentation-record baseline before the merge.
- The merge is developed and validated only on `sync/upstream-47348d1` in its isolated worktree.
- The dirty-work snapshot is retained until restoration and hash/status verification complete.
- No destructive Git command is permitted.

## Errors encountered

| Date | Error | Resolution |
|---|---|---|
| 2026-07-26 | Initial remote Agent was created under `/Users/Shared/Dev/AI/pi-grok-build`, an empty non-repository path | Cancelled that session before any repository mutation and relaunched at `/Users/dengwenyu/Dev/AI/pi-grok-build` |
| 2026-07-26 | A `/tmp` fact file was not visible to the subsequent read tool | Re-read the immutable Git commit message directly with fixed-width output; no repository state was affected |

## Status log

- **[2026-07-26]** Preflight complete; upstream `47348d1` fetched and authoritative change list recorded.
- **[2026-07-26]** Existing dirty work captured in stash object `97f443b...`; clean isolated worktree `sync/upstream-47348d1` created.
