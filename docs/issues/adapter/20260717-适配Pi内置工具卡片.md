---
id: "2026-07-17-adapt-pi-builtin-tools"
title: "适配 Pi 内置工具卡片 (read/bash/edit/write/grep/find/ls)"
status: "completed"
created: "2026-07-17"
updated: "2026-07-17"
category: "adapter"
tags: ["tools", "pi", "tool-card"]
---

# Issue: 适配 Pi 内置工具到 Grok 原生卡片

## Goal

让 Pi 全部内置工具在 Grok 原生 tool cards 中正确显示：

| Pi tool | Target Grok card |
|---|---|
| read | Read |
| bash | Execute |
| edit | Edit (diff hunks) |
| write | Edit + "Creating " |
| grep | Search (structured matches) |
| find | Search (files with matches) |
| ls | ListDir |

## Prior art

- read/bash typed raw_output: completed (`20260717-修复 Pi read 与 bash 工具卡片.md`)
- edit multi-diff: completed (`架构/20260717-修复 Pi edit 工具展开.md`)

## Implemented

- `normalize_tool_raw_input`: write→variant Write; ls→target_directory; find→output_mode; grep→`-i`
- `normalize_tool_raw_output`: grep→GrepSearch; find→GrepSearch files; ls→ListDir
- Pager: `is_write_tool` also matches title `write`; search meta reads `ignoreCase` / `glob_pattern`
- Tests: 29 passed in `pi-grok-adapter`

## Architecture

All projection stays in `pi-grok-adapter` (headless). Pager only needs tiny recognition fixes.
